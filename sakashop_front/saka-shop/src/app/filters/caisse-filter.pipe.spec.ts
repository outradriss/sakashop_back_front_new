import { CaisseFilterPipe } from './caisse-filter.pipe';

describe('CaisseFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CaisseFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
