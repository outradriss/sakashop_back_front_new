export interface Vente {
  name: string;           // Produits Vendus
  quantity: number;       // Quantité Vendus
  buyPrice: number;       // Prix d'Achat
  salesPrice: number;     // Prix de Vente
  productAddedDate: string; // Date
}
